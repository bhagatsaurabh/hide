export const CompleteProfile = () => {
  // TODO: Accept new username like in Auth and try to create user profile doc in firestore until success
  // TODO: Once successful, move to /dashboard
  // Ext TODO: The gateway needs to verify that the profile is created before allowing any apis
  return <></>;
};
